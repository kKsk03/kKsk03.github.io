# `kKsk03.github.io`

文档使用：[VuePress Theme Plume](https://theme-plume.vuejs.press/)  
已对接域名，直接访问：[kKsk's Blog](https://kksk03.site/)  

## 编写文章

要求：`Node.js` 版本为 `20.19.0` 或大于等于 `22.0.0`  
源码拉取后，需使用 `pnpm install` 安装依赖。  

随后，使用 `pnpm dev` 启动开发服务器，使用 `pnpm build` 进行构建。  